## Problem

What is wrong or missing today, and why it matters.

## Implementation

What this pull request does. Name the modules it touches, and any dependency rule it
relies on.

## Spec coverage

Which OpenSpec change this belongs to, and which scenarios it satisfies.
Example: `bootstrap-project-skeleton`, `app-shell` -> "Each tab keeps its own state".

## Trade-offs

What I picked, what I turned down, and why. Delete this section if there was no real
choice to make.

## Test steps

How a reviewer checks this by hand. Delete it if the unit tests cover the change fully.

## Left out

Work split off on purpose to keep this pull request small enough to review. Delete if
there is none.
